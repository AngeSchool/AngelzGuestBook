@model GuestbookApp.Models.Message

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Message</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Content" class="control-label"></label>
                <textarea asp-for="Content" class="form-control" rows="3"></textarea>
                <span class="form-text">Du kan använda emojis! 😀🎉❤️</span>
                <span asp-validation-for="Content" class="text-danger"></span>

                <div class="mt-2">
                    <button type="button" class="btn btn-light" onclick="insertEmoji('😄')">😄</button>
                    <button type="button" class="btn btn-light" onclick="insertEmoji('🎉')">🎉</button>
                    <button type="button" class="btn btn-light" onclick="insertEmoji('❤️')">❤️</button>
                    <button type="button" class="btn btn-light" onclick="insertEmoji('✨')">✨</button>
                </div>

                <div class="form-group mt-2">
                    <label for="emojiDropdown">Välj en emoji:</label>
                    <select id="emojiDropdown" class="form-select" onchange="insertEmoji(this.value)">
                        <option value="">-- Välj --</option>
                        <option value="😀">😀 Glad</option>
                        <option value="😂">😂 Skratt</option>
                        <option value="😍">😍 Kärlek</option>
                        <option value="🎉">🎉 Fest</option>
                        <option value="😢">😢 Ledsen</option>
                        <option value="👍">👍 Tumme upp</option>
                        <option value="❤️">❤️ Hjärta</option>
                        <option value="✨">✨ Glans</option>
                        <option value="😎">😎 Cool</option>
                        <option value="🙌">🙌 Hurra</option>
                    </select>
                </div>

            </div>
            <div class="form-group">
                <label asp-for="PostedAt" class="control-label"></label>
                <input asp-for="PostedAt" class="form-control" />
                <span asp-validation-for="PostedAt" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        function insertEmoji(emoji) {
            if (!emoji) return;
            const textarea = document.querySelector('[name="Content"]');
            if (textarea) {
                const start = textarea.selectionStart ?? 0;
                const end = textarea.selectionEnd ?? 0;
                const text = textarea.value;
                textarea.value = text.substring(0, start) + emoji + text.substring(end);
                textarea.focus();
                textarea.selectionStart = textarea.selectionEnd = start + emoji.length;
            }
            const dd = document.getElementById("emojiDropdown");
            if (dd) dd.selectedIndex = 0;
        }
    </script>
}